import { PodcastsFilterPipe } from './podcasts-filter.pipe';

describe('PodcastsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PodcastsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
